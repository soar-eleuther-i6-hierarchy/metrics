# Server credentials / access notes — never commit
.ssh
.server-access.md
.env

# SSH keys
*.pem
id_ed25519
id_ed25519.pub
id_rsa
id_rsa.pub

# Python
__pycache__/
*.py[cod]
venv/
.venv/
*.egg-info/
.pytest_cache/
outputs/*.log

# Stage-01 tensor caches (~700 MB/layer). Hosted on the Hub instead, see README:
# huggingface.co/datasets/soar-eleuther-i6-hierarchy/experiment_0-stats
outputs/**/*.pt
outputs/*.pt
# The token cache's shards are .pt and already covered; its meta.json is not, and
# a manifest for shards nobody has is worse than nothing.
outputs/**/token_cache/

# Generated proof-figures (rebuilt by reporting/make_report_figures.py into each
# run dir's figures/; never tracked)
# figures/

# Generated toy-world visualization (rebuilt from the generator; a viz artifact, not source)
toygen/*.html

# OS
.DS_Store

toygen/DESIGN.md
training/_fetched
tests_local/
outputs_local/
outputs_archive/pilot_seed0
design_studies/
synthdict/tests

/KNOWN_BUGS.md
*.pdf
scoring/MATCHING_METHODS.md
synthdict/*.md
/scripts

# Transitional: a one-shot 5.x->6.x checkpoint-boundary probe, retired once training
# moves to the sae-training repo. Kept local, not tracked.
training/roundtrip_check.py
# Sibling repos (MEETING_NOTES/, PCFG/, sae-training/) live BESIDE experiment_0,
# not inside it — nothing to ignore here. Tier-2 calibration finds the
# sae-training clone at ../sae-training or via EXP0_SAE_TRAINING.
